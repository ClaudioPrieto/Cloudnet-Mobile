# Cloudnet Mobile App

## Setup
Install Yarn
```bash
  npm install --global yarn
```

Install node OSX
```bash
  brew install node
```

Install expo-cli
```bash
  yarn add expo-cli
```

Run project (In root directory)
```bash
  yarn start
```